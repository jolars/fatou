name: Publish Crates

on:
  workflow_dispatch:
  push:
    tags:
      # The member tags are included so a library-only release publishes
      # immediately; `--skip-published` makes overlapping runs idempotent.
      - "v*"
      - "fatou-parser-v*"
      - "fatou-formatter-v*"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  publish:
    name: Publish to crates.io
    runs-on: ubuntu-latest
    environment: release
    permissions:
      id-token: write # Required for OIDC token exchange
    steps:
      - uses: actions/checkout@v7

      - uses: rust-lang/crates-io-auth-action@v1
        id: auth

      - name: Install Rust
        run: rustup toolchain install

      - name: Install cargo-workspaces
        run: cargo install cargo-workspaces --locked

      # Publishes in dependency order (fatou-parser, fatou-formatter, fatou);
      # `--from-git` takes the versions at this commit and `--skip-published`
      # silently skips crates already on crates.io. Verification is deliberately
      # left on: it builds each crate against its *registry* dependencies, which
      # is the only place a stale published manifest (path deps mask it locally)
      # shows up before users hit it with `cargo install`.
      - name: Publish workspace
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}
        run: cargo workspaces publish --from-git --skip-published --yes
