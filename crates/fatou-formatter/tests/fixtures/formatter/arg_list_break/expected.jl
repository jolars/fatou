short(a, b)
register_handler(
    event_source,
    callback_function,
    priority_level,
    retry_policy,
    request_timeout_ms,
)
nested(
    first_outer_argument,
    inner(alpha_one, beta_two),
    second_outer_argument,
    third_outer_argument,
)
