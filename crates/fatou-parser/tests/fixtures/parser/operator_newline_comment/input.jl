op = -  # a value, not a prefix call
result = op(a, b)
1: # the range ends here
2
/ # a binary-only operator stays a value
z
[1: # a row separator still follows
2]
(1: # newlines are insignificant in parens
2)
x = 1: #= trivia, not a line end =# 2
