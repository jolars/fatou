# @fatou-cli/**NAME**

This is an internal platform-specific package containing a prebuilt
[fatou](https://github.com/jolars/fatou) binary for `__NAME__`.

You should not depend on it directly. Install
[`fatou-cli`](https://www.npmjs.com/package/fatou-cli) instead---it will pull in
the right binary for your platform automatically:

```sh
npm install -g fatou-cli
```
