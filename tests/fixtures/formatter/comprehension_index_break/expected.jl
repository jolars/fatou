flat = [f(x) for x in xs][i]
fits_at_ninety_two = [transform(element) for element in medium_collection_namings][chosen_k]
breaks_at_ninety_three = [
    transform(element)
    for element in medium_collection_ids
][chosen_ky]
filtered = [
    transform(element)
    for element in collection_of_input_values
    if is_valid(element)
][index]
chain = [
    combine(left, right)
    for (left, right) in zip(first_sequence, second_sequence)
][key_one][key_two]
gen = (
    accumulate(item)
    for item in extremely_long_iterable_name_for_testing
    if wanted(item)
)[1]
braces = {
    pair_of(item)
    for item in source_items_collection
    if accept(item) && wanted(item)
}[k]
hugged = process(configuration, [
    normalize(record)
    for record in all_incoming_records
])[selected]
pairhug = lookup(key_prefix => [
    expand(entry)
    for entry in configuration_entries_by_name
])[key]
both = [
    make(item)
    for item in items_source
][
    first_tremendously_long_offset_selector_expression,
    second_tremendously_long_offset_selector_expr,
]
typed = Float64[
    convert_to_float(raw_measurement)
    for raw_measurement in sensor_readings
][idx]
reflowed = [f(x) for x in xs][i]
