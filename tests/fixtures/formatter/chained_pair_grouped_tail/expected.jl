dict = Dict(
    first_key_name =>
        second_key_name =>
        alpha_term + beta_term + gamma_term + delta_term_xxxxx,
)
vect = [
    outer_label_name =>
        inner_label_name =>
        (alpha_value + beta_value + gamma_value + delta_valuex),
]
kwarg = build(
    base_arg,
    opts = primary_key =>
        secondary_key =>
        alpha_bit + beta_bit + gamma_bit + delta_bit_xxxxxx,
)
deep = Dict(
    first_key_name =>
        second_key_name =>
        alpha_termmmm +
            beta_termmmm +
            gamma_termmmm +
            delta_termmmm +
            epsilon_termmmm +
            zeta_termmmm,
)
leaf = Dict(some_key_name => other_key_name => plain_leaf_tail_value_without_a_bracket_x)
prebroken = Dict(
    first_key_name =>
        second_key_name =>
        alpha_term + beta_term + gamma_term + delta_term_xxxxx,
)
