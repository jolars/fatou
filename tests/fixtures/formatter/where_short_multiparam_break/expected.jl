function getindex(
    df::DataFrame,
    row_indices::AbstractVector{T},
    columns::Colon,
) where {T, S}
    return new_df
end
transform(
    input::Matrix,
    weights::Vector,
    biases::Vector,
    opts::Options,
) where {S, T, U} = go
foo(x::S, y::T) where {S, T} = value
